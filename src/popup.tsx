import { PopupApplication } from './_shared/apps/PopupApplication/PopupApplication';
import { renderApp } from './_shared/utils/renderApp';

renderApp({
  appId: 'root',
  Application: PopupApplication,
});
