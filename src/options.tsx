import { OptionsApplication } from './_shared/apps/OptionsApplication/OptionsApplication';
import { renderApp } from './_shared/utils/renderApp';

renderApp({
  appId: 'root',
  Application: OptionsApplication,
});
